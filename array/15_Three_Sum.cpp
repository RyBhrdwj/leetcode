#include<iostream>
#include<vector>
using namespace std;

// QUESTION DATA :: From integer array nums, return triplet [nums[i], nums[j], nums[k]]
//                  such that i != j, i != k and j != k & nums[i] + nums[j] + nums[k] == 0.

class Solution {
public:
    // Approach 1 : Brute Force
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> triplets;
        
    }
};

int main()
{
    
    return 0;
}